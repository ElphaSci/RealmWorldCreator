[project]
name = "realm-world-creator"
version = "0.1.0"
description = ""
authors = [
    {name = "cmhulbert",email = "cmhulbert@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.10"
package-mode = false

dependencies = [
    "Pillow",
    "FreeSimpleGUI",
    "ipython",
    "matplotlib",
    "rapidfuzz",
    "appdirs",
    "kaitaistruct==0.10",
    "antlr4-python3-runtime",
    "pyinstaller",
    "kaitaisci @ git+https://github.com/ElphaSci/KaitaiSci.git"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
